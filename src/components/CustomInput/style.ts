import { StyleSheet } from 'react-native';

const styles =  StyleSheet.create({
    input: {
        borderColor: 'green',
        borderWidth: 1,
        borderRadius: 10,
        width: 350,
        margin: 10,
        padding: 15,
        height: 55,
        fontSize: 17
    },
});

export default styles;
