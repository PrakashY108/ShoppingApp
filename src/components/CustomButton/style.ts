import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  btn: {
    width: 350,
    height: 50,
    borderRadius: 50,
    borderColor:"black",
    borderWidth:1
  },
  btnText: {
    alignItems: 'center',
    alignSelf: 'center',
  },
});

export default styles;
