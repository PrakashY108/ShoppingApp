import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignContent: 'center',
    alignItems: 'center',
  },
  forgot: {
    gap: 7,
  },
  logo: {
    height: 150,
    width: 150,
  },
  logocontainer: {
    height: 180,
    width: 180,
    borderRadius: 90,
  },
});

export default styles;
